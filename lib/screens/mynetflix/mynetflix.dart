import 'package:flutter/material.dart';

class Mynetflix extends StatelessWidget {
  const Mynetflix({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('MyNetflix'),
    );
  }
}